/* ============================================================
   Buttons
============================================================ */

export const buttonHover = {
  y: -2,
  scale: 1.02,

  transition: {
    duration: 0.2,
    ease: "easeOut",
  },
};

export const buttonTap = {
  scale: 0.98,

  transition: {
    duration: 0.12,
    ease: "easeOut",
  },
};

/* ============================================================
   Cards
============================================================ */

export const cardHover = {
  y: -6,

  transition: {
    duration: 0.25,
    ease: "easeOut",
  },
};

export const cardTap = {
  scale: 0.995,

  transition: {
    duration: 0.15,
  },
};

/* ============================================================
   Small Cards
============================================================ */

export const smallCardHover = {
  y: -3,

  transition: {
    duration: 0.2,
    ease: "easeOut",
  },
};

/* ============================================================
   Icons
============================================================ */

export const iconHover = {
  scale: 1.1,

  transition: {
    duration: 0.18,
    ease: "easeOut",
  },
};

export const iconRotateHover = {
  rotate: 8,
  scale: 1.08,

  transition: {
    duration: 0.2,
    ease: "easeOut",
  },
};

/* ============================================================
   Images
============================================================ */

export const imageHover = {
  scale: 1.03,

  transition: {
    duration: 0.35,
    ease: "easeOut",
  },
};

/* ============================================================
   Dashboard Widget
============================================================ */

export const widgetHover = {
  y: -4,
  scale: 1.01,

  transition: {
    duration: 0.25,
    ease: "easeOut",
  },
};

/* ============================================================
   FAQ Category Card
============================================================ */

export const faqCardHover = {
  y: -4,

  transition: {
    duration: 0.2,
    ease: "easeOut",
  },
};

/* ============================================================
   Navigation Link
============================================================ */

export const navItemHover = {
  y: -1,

  transition: {
    duration: 0.15,
    ease: "easeOut",
  },
};

/* ============================================================
   Social Icons
============================================================ */

export const socialHover = {
  y: -2,
  scale: 1.08,

  transition: {
    duration: 0.2,
    ease: "easeOut",
  },
};

/* ============================================================
   Floating Badge
============================================================ */

export const badgeHover = {
  scale: 1.04,

  transition: {
    duration: 0.18,
    ease: "easeOut",
  },
};